import { db } from "../db";

class ProfesorModel{
    static async RegisterProfesor({id,fecha_ingreso,dedicacion,categotia,area_de_conocimiento,pre_grado,post_grado}){
        const responce = db.query('')
    }
}